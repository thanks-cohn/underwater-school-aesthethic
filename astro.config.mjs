import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://thanks-cohn.github.io',
  base: '/underwater-school-aesthethic',
  output: 'static',
  trailingSlash: 'always'
});
