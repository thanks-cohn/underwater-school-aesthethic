# Room backgrounds

Put room-specific background assets here.

Use the room slug as the filename basename. Examples:

- `classroom.webp`
- `library.jpg`
- `pool-wing.png`
- `assembly-hall.webm`

The extension is auto-detected during the Astro build. If a matching local file is absent, the room falls back to its configured remote URL.
